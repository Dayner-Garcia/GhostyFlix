@model IEnumerable<Application.App_Management.ViewModels.GendersViewModel>

<h1 class="mb-4">Mantenimiento de Generos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary mb-3">Crear Nuevo Genero</a>
</p>

@if (!Model.Any())
{
    <div class="alert alert-info text-center" role="alert">
        <p>No hay generos registrados en este momento.</p>
    </div>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
        <tr>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var genre in Model)
        {
            <tr>
                <td>@genre.Name</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@genre.Id" class="btn btn-warning">Editar</a>
                    <a asp-action="Delete" asp-route-id="@genre.Id" class="btn btn-danger">Eliminar</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}